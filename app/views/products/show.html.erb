<h1 class='title'><%= @product.title %></h1>

<p class='description'> <%= @product.description %></p>

<span class='price'> <%= @product.price %></span>

<div>
    <br>
    <%= link_to 'Edit product', edit_product_path(@product), data: { confirm: '¿Estas seguro?' }, class: 'btn btn-warning'  %>
</div>

<div>
    <br>
    <%= link_to 'Delete product', product_path(@product), data: {turbo_method: :delete}, class: 'btn btn-warning'  %>
</div>

