<div id="<%= dom_id category %>">
  <%= category.name %>
  <div>
      <%= link_to 'Edit Category', edit_category_path(category), class: 'btn btn-warning'%>
      <%= button_to "Delete product", category_path(category), method: :delete, class: 'btn btn-danger'%>
  </div>

</div>
